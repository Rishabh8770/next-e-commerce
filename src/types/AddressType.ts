export type AddressType = {
  firstName: string;
  lastName: string;
  address: string;
  subAddress: string;
  city: string;
  state: string;
  postalCode: string;
  country: string;
  phone: string;
};
